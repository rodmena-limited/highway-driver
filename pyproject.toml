[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "highway"
version = "0.9.1"
description = "Python SDK for Highway Workflow Engine - build durable workflows with simple decorators"
readme = "README.rst"
license = "Apache-2.0"
requires-python = ">=3.11"
authors = [
    {name = "Highway Team", email = "team@highway.solutions"}
]
keywords = ["workflow", "orchestration", "highway", "durable", "execution", "sdk"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Distributed Computing",
]

dependencies = [
    "stabilize>=0.9.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "mypy>=1.0",
    "ruff>=0.1",
]

[project.urls]
Homepage = "https://highway.solutions"
Documentation = "https://docs.highway.solutions"
Repository = "https://github.com/rodmena-limited/highway-driver"
Issues = "https://github.com/rodmena-limited/highway-driver/issues"
Changelog = "https://github.com/rodmena-limited/highway-driver/releases"

[tool.setuptools.packages.find]
where = ["."]
include = ["highway*"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "acceptance: marks tests as acceptance tests (require API)",
    "unit: marks tests as unit tests (no API required)",
]
addopts = "-v"

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
